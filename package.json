{
  "name": "orbit-cards",
  "version": "1.2.0-beta.3",
  "scripts": {
    "build": "vite build",
    "build:dev": "vite build && vite build --mode orbit-dev",
    "test": "node --test test/*.test.js",
    "watch": "vite build --watch",
    "watch:dev": "vite build --mode orbit-dev --watch"
  },
  "description": "A collection of modern, configurable Home Assistant dashboard cards and badges with shared editors, layouts, templates, colors, icons, and interactions.",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
